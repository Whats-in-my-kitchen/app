import { StyleSheet } from "react-native";

const StylesWelcomeScreen = StyleSheet.create({
  container: {
    display: "flex",
    alignItems: "center",
    padding:20,
    justifyContent: "center",
  },
});

export default StylesWelcomeScreen;
